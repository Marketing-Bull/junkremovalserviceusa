// Registers jest-dom matchers (toBeInTheDocument, toBeDisabled, …) and, with
// `globals: true`, enables @testing-library/react's automatic cleanup between
// tests so component trees don't leak across test cases.
import "@testing-library/jest-dom/vitest"
